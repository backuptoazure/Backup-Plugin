function deleteallConfirm(){
  var r=confirm("Are you sure you wanna delete all the backups ?");
    if(r)
	  return true;
    return false;    
    }
function deleteConfirm(){
  var r=confirm("Are you sure you wanna delete this backup ?");
    if(r)
	  return true;
    return false;    
    }
function new_start()
{
alert("On some versions of windows server progress bar might not be visible AND Please do not stop the page until the backup is complete ");
}

